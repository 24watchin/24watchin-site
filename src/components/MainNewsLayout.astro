---
const API_KEY = import.meta.env.API_KEY;
const BLOG_ID = import.meta.env.BLOG_ID;
const url = `https://www.googleapis.com/blogger/v3/blogs/${BLOG_ID}/posts?key=${API_KEY}&maxResults=20`;

const response = await fetch(url);
const data = await response.json();
const posts = data.items || [];

const getImageUrl = (content) => {
  const match = (content || '').match(/<img[^>]+src="([^">]+)"/);
  return match ? match[1] : 'https://placehold.co/600x400/eeeeee/999999?text=No+Image';
};

const getExcerpt = (content) => {
  if (!content) return "";
  return content.replace(/<[^>]*>/g, '').substring(0, 100) + '...';
};
---

<div class="top-news-container">
  <div class="main-column">
    {posts.length > 0 && (
      <article class="hero-post">
        <a href={`/news/${posts[0].id}/`}>
          <div class="hero-image">
            <img src={getImageUrl(posts[0].content)} alt={posts[0].title} />
          </div>
          <div class="hero-text">
            <span class="category-red">Region</span>
            <h2>{posts[0].title}</h2>
            <p>{getExcerpt(posts[0].content)}</p>
          </div>
        </a>
      </article>
    )}

    <div class="sub-grid">
      {posts.slice(1, 5).map((post) => (
        <article class="grid-item">
          <a href={`/news/${post.id}/`}>
            <div class="grid-image">
              <img src={getImageUrl(post.content)} alt={post.title} />
            </div>
            <div class="grid-text">
              <span class="category-red small">Region</span>
              <h3>{post.title}</h3>
            </div>
          </a>
        </article>
      ))}
    </div>
  </div>

  <div class="side-list-column">
    {/* ▼ 記事を8件（5番目〜13番目）に増やしました ▼ */}
    {posts.slice(5, 13).map((post) => (
      <article class="list-item">
        <a href={`/news/${post.id}/`}>
          <h4>{post.title}</h4>
        </a>
      </article>
    ))}
  </div>
</div>

<style>
  /* ▼ 右側の幅を400px（左右で約半分ずつ）に広げました ▼ */
  .top-news-container { display: grid; grid-template-columns: 1fr 400px; gap: 30px; margin-bottom: 40px; }
  @media (max-width: 900px) { .top-news-container { grid-template-columns: 1fr; } }
  .category-red { color: #b91c1c; font-weight: bold; font-size: 14px; margin-bottom: 5px; display: block; }
  .category-red.small { font-size: 12px; }
  .category-red.mini { font-size: 11px; display: inline; margin-right: 5px; }
  
  .hero-post a { display: grid; grid-template-columns: 1fr 1.2fr; gap: 20px; text-decoration: none; color: #333; margin-bottom: 15px; }
  .hero-image { aspect-ratio: 16/10; overflow: hidden; border-radius: 4px; }
  .hero-image img { width: 100%; height: 100%; object-fit: cover; }
  .hero-text h2 { font-size: 22px; margin: 5px 0 10px; line-height: 1.3; }
  .hero-text p { font-size: 14px; color: #666; line-height: 1.6; }
  
  .sub-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; border-top: 1px solid #eee; padding-top: 15px; }
  .grid-item a { display: flex; gap: 15px; text-decoration: none; color: #333; }
  .grid-image { width: 120px; height: 80px; flex-shrink: 0; overflow: hidden; border-radius: 4px; }
  .grid-image img { width: 100%; height: 100%; object-fit: cover; }
  .grid-text h3 { font-size: 14px; margin: 0; line-height: 1.4; }
  
  .side-list-column { border-left: 1px solid #eee; padding-left: 20px; }
  .list-item { border-bottom: 1px solid #eee; padding: 10px 0; }
  .list-item:first-child { padding-top: 0; }
  .list-item a { text-decoration: none; color: #333; }
  
  .list-item h4 { display: inline; font-size: 14px; line-height: 1.25; font-weight: normal; margin: 0; display: block; }
  
  @media (max-width: 600px) { 
    .hero-post a { grid-template-columns: 1fr; } 
    .sub-grid { grid-template-columns: 1fr; } 
    .side-list-column { border-left: none; padding-left: 0; border-top: 1px solid #eee; margin-top: 20px; } 
  }
</style>
