---
const API_KEY = import.meta.env.API_KEY;
const BLOG_ID = import.meta.env.BLOG_ID_JA;

const url = `https://www.googleapis.com/blogger/v3/blogs/${BLOG_ID}/posts?key=${API_KEY}&maxResults=50`;

const response = await fetch(url).catch(() => null);
const data = response ? await response.json() : { items: [] };
const allPosts = data.items || [];

const isForeignPost = (post) => {
  if (!post.labels) return false;
  return post.labels.some(label => /^[a-z]{2}-ja$/.test(label) && label !== 'jp-ja');
};

const posts = allPosts.filter(post => !isForeignPost(post)).slice(0, 10);

const getImageUrl = (content) => {
  const match = (content || '').match(/<img[^>]+src="([^">]+)"/);
  return match ? match[1] : 'https://placehold.co/300x200/eeeeee/999999?text=Trend';
};
---

<section class="bing-trend-section">
  <div class="trend-header">
    <h3>トレンドWatchin！</h3>
  </div>

  <div class="trend-scroll-outer">
    <div class="scroll-arrow prev-arrow" id="btn-prev">
      <i class="fas fa-chevron-left"></i>
    </div>

    <div class="trend-scroll-container" id="trend-scroll">
      {posts.map((post) => (
        <a href={`/ja/jp/news/${post.id}/`} class="trend-item">
          <div class="trend-img-wrapper">
            <img src={getImageUrl(post.content)} alt={post.title} />
          </div>
          <h4 class="trend-title">{post.title}</h4>
        </a>
      ))}
    </div>

    <div class="scroll-arrow next-arrow" id="btn-next">
      <i class="fas fa-chevron-right"></i>
    </div>
  </div>
</section>

<style>
  .bing-trend-section { padding: 10px 0 20px 0; background: #fff; }
  .trend-header h3 { font-size: 24px; font-weight: bold; border-bottom: 4px solid #8b0000; padding-bottom: 5px; }
  .trend-scroll-outer { position: relative; display: flex; align-items: center; }
  .trend-scroll-container { display: flex; gap: 20px; overflow-x: auto; scroll-behavior: smooth; }
  .trend-item { min-width: 180px; text-decoration: none; color: #333; }
  .trend-img-wrapper { width: 100%; height: 120px; border-radius: 8px; overflow: hidden; }
  .trend-img-wrapper img { width: 100%; height: 100%; object-fit: cover; }
  .trend-title { font-size: 16px; font-weight: bold; margin-top: 10px; }
  .scroll-arrow { position: absolute; background: #fff; width: 40px; height: 40px; border-radius: 50%; display: flex; align-items: center; justify-content: center; box-shadow: 0 2px 8px rgba(0,0,0,0.2); cursor: pointer; }
  .prev-arrow { left: -15px; } 
  .next-arrow { right: -15px; } 
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const scrollContainer = document.getElementById('trend-scroll');
    const btnPrev = document.getElementById('btn-prev');
    const btnNext = document.getElementById('btn-next');
    if (scrollContainer && btnPrev && btnNext) {
      btnNext.addEventListener('click', () => scrollContainer.scrollBy({ left: 400, behavior: 'smooth' }));
      btnPrev.addEventListener('click', () => scrollContainer.scrollBy({ left: -400, behavior: 'smooth' }));
    }
  });
</script>
