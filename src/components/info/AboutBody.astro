<style>
  /* 中略（カードの設定などはそのまま） */
  
  /* トップの大見出し：24px (1.5rem) に設定 */
  h1 { font-size: 1.5rem; font-weight: bold; margin-bottom: 30px; padding-bottom: 15px; border-bottom: 4px solid var(--theme-color); color: #1a1a1a; }
  
  /* ★本文の基本サイズ：16px (1rem) に修正 */
  .info-body-text { font-size: 1rem; line-height: 1.8; color: #444; }
  
  /* セクションの中見出し：20px (1.25rem) に設定 */
  .info-body-text :global(h2) { font-size: 1.25rem; margin-top: 30px; border-left: 4px solid var(--theme-color); padding-left: 10px; }

  /* 以下略 */
</style>
